name(x,y)  {
    if (x prefix cond_repeat("y", "y"))
        if ("y" prefix x)
            x
        else
            if ("4" prefix x)
                fullname(name(name(surname(reverse "compilers"), "u"), "id"), " ", surname(reverse reverse "smara"))
            else
                reverse reverse reverse x
    else
        "5"
}

surname(x) {
    x+name("e"+name("r", "tree")+"vasia", x)+"vasia"
}

fullname(first_name, sep, last_name) {
    first_name + sep + last_name
}

cond_repeat(c, x) {
    if (c prefix "yes")
        if("yes" prefix c)
            surname(x)
        else
            x
    else
        x
}

newf(){
    if ("F" prefix "Fox") "cool" else "error" + if ("B" prefix "Bear") "nice" else "not nice"
}

newf()
name(surname("x"), surname("y"))
if ("yes" prefix "yes!") "hellooo" else "its me" + if ("yes" prefix "yes!") "hellooo" else "its me"
surname(reverse reverse reverse "x")
fullname(name(name(surname("w"), "i"), "p"), " ", surname("v"))
