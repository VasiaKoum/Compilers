name()  {
    if ("rerer")
    "ererer"
    else "sdede"
}

surname(x) {
    x+x
}

fullname(first_name, sep, last_name) {
    first_name + sep + last_name
}

name()
surname()
fullname(name(name(surname())), " ", surname())
